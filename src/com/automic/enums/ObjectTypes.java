package com.automic.enums;

public enum ObjectTypes {
	//JOBS, JOBP, SCRI, VARA, CALL, QUEUE, LOGIN, JOBF, CONN, HOSTG, FILTER, CALE, USRG, EVNT, FOLD, PRPT
	/*  JOBS("JOBS"),
	  JOBP("JOBS"),
	  SCRI("JOBS"),
	  VARA("JOBS"),
	  CALL("JOBS"),//
	  QUEUE("JOBS"),
	  LOGIN("JOBS"),
	  JOBF("JOBS"),//
	  CONN("JOBS"),//
	  HOSTG("JOBS"),
	  FILTER("JOBS"),
	  CALE("JOBS"),
	  USRG("JOBS"),
	  EVNT("JOBS"),
	  FOLD("JOBS"),
	  CLIENT("com.automic.enums.ClientActionTypes"),
	  PRPT("JOBS");
	  
	  
	  private String objName;
		
	    private ObjectTypes(String objName) {
	        this.objName = objName;
	}
	    public String getActionTypeClass() {
	        return this.objName;
	        }
	    
	public static boolean contains(String str) {
		    for (ObjectTypes c : ObjectTypes.values()) {if (c.name().equals(str)) {return true;}}
		    return false;
		}
		*/
}
